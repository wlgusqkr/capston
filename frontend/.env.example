# Vite는 VITE_ 프리픽스가 붙은 변수만 클라이언트에 노출됩니다.

# Backend API base URL
VITE_API_BASE_URL=http://localhost:8000/api

# 카카오 JS 키 (시간 남으면 사용)
VITE_KAKAO_JS_KEY=

# 지도 타일 (선택 — Leaflet은 OSM 기본 사용. 필요 시 별도 키)
VITE_MAP_TILE_URL=
