# QA Review: 4단계 메인 지도 (Leaflet 히트맵 + 사이드바)

리뷰 모드: **Mode B (Frontend Screen Review) + Mode E (Cross-Screen Consistency)**
리뷰어: 메인 코디네이터
일자: 2026-05-02

## Verdict

**PASS WITH NOTES** — 1건 자체 수정 후 통과. typecheck/build/E2E (CORS+API) 모두 그린.

## Scope

### 검토 파일
- 셋업: `frontend/{package.json, vite.config.ts, tsconfig.app.json, index.html, src/main.tsx, src/App.tsx}`
- 데이터: `src/{lib/api.ts, lib/weights.ts, lib/geo.ts, hooks/useDongs.ts, types/api.ts}`
- 라우트: `src/routes/{MainMap.tsx + .css, NotFound.tsx}`
- 지도 컴포넌트: `src/components/Map/{HeatMap, Sidebar, Legend, ViewToggle}.tsx + .css`

### Ground truth
- SPEC.md 섹션 4 (디자인 시스템), 6.1 (메인 지도), 8 (URL), 9 (API)
- `CLAUDE.md` localStorage 금지 등
- 핸드오프 step3 (백엔드 API 응답 형식)

## 정적 + 런타임 검증

| 검사 | 결과 |
|---|---|
| `tsc --noEmit` | ✅ 0 errors |
| `npm run build` | ✅ 220 modules, 407KB JS / 41KB CSS (gzip 130KB / 11KB) |
| Hex 하드코딩 in feature code | ⚠ **1건 → 자체 수정 완료** |
| Hex 하드코딩 in feature CSS | ✅ 0건 |
| `font-size: ...px` 하드코딩 | ✅ 0건 |
| `localStorage` / `sessionStorage` | ✅ 0건 |
| TanStack Query 사용 | ✅ `useDongScores` |
| API base = `import.meta.env.VITE_API_BASE_URL` | ✅ |
| Vite 프록시 `/api` → `http://localhost:8000` | ✅ 폴백 경로 |
| dev 서버 (`localhost:5173`) | ✅ HTTP 200, `<html lang="ko">`, React 마운트 |
| 백엔드 API 호출 (실서버 + Origin 헤더) | ✅ 200 + CORS 헤더 정확 |

## Findings

### Blockers
- 없음.

### Notes (자체 수정 또는 트래킹)

#### N1. ~~`HeatMap.tsx:51` 폴리곤 stroke `'#FFFFFF'` 하드코딩~~ — 수정 완료
- **위치**: `frontend/src/components/Map/HeatMap.tsx:51`
- **문제**: Leaflet pathOptions의 `color` (= 폴리곤 stroke)에 hex 직접 박힘.
- **명세 근거**: SPEC 4 디자인 시스템 = "디자인 토큰만 사용, 하드코딩 금지". CLAUDE.md "디자인 토큰만 사용".
- **조치**: `lib/colors.ts`에 `MAP_POLYGON_STROKE = { light: '#FFFFFF', dark: '#3A3A36' }` 추가 (기존 HEATMAP_COLORS 패턴 따름 — Leaflet은 CSS var 못 받음). HeatMap.tsx에서 `MAP_POLYGON_STROKE.light` import 사용. 다음 단계에서 다크 모드 자동 매핑은 별도 작업.
- typecheck 통과.

#### N2. 가중치 변경 시 백엔드 재호출
- **상황**: 슬라이더 변경 → queryKey 변경 → React Query 자동 refetch.
- **SPEC 14.3** "가중치 슬라이더 변경 시 색상 재계산은 클라이언트에서 (서버 왕복 X)"와 충돌.
- **이유**: 현재 `/api/dongs/scores` 응답에 `score_rent/amenity/transit` 분해 필드 없음 (weighted score만 옴).
- **조치**: 5단계에서 백엔드 응답 확장 필요(아래 N4 참조). 현 단계는 5개 동이라 부담 없음.

#### N3. 더미 폴리곤 (`boundingPolygon`)
- 5개 동의 GeoJSON이 없어 클라가 `lat, lng` 주변 ~500m 사각형을 그림. 시각이 단조롭지만 가중치 효과 데모는 충분.
- 10단계(data-pipeline)에서 실제 행정동 폴리곤 적재 시 `geo.ts` 헬퍼는 제거 또는 fallback로 유지.

#### N4. 5단계로 미룬 백엔드 작업
- `/api/dongs/:slug/summary` 신규 (SPEC 6.2)
- 가능하면 `/api/dongs/scores` 응답에 `score_rent/score_amenity/score_transit` 추가 → 클라 재계산 가능 → SPEC 14.3 충족
- 5단계 backend-engineer 위임 시 핸드오프에 명시.

#### N5. UI placeholder 영역
- 검색 자동완성, 필터(rent cap, near university), 레이어 탭(composite/rent/amenity/transit), 3D 토글: **상호작용 미구현**, 시각만 존재.
- 5단계 이후 wire up. 4단계 명세는 "UI만 / placeholder OK"라 PASS.

### Good

- ✅ TanStack Query queryKey에 weights 객체 직접 포함 → 가중치 변경 시 자동 refetch (캐시 키 정확).
- ✅ `lib/weights.ts`의 `rebalanceWeights`: 한 슬라이더 변경 → 나머지 둘 비례 분배 + 합 100 보장 (반올림 오차 흡수). 백엔드 검증(`abs(sum-100)>1` → 400)과 일관.
- ✅ Leaflet 호버 시 fillOpacity 0.6 → 0.85, weight 1 → 2 (사용자 피드백 명확).
- ✅ 한국어 텍스트 일관: "동네 점수를 불러오는 중…", "데이터를 불러오지 못했습니다.", aria-label "서울 동네 히트맵".
- ✅ 디자인 시스템 일관: `<Slider />`, `<Button />` 등 ui/ 프리미티브 사용. 새 색/크기 정의 없음.
- ✅ `<html lang="ko">` 명시. SEO/접근성 기본 OK.
- ✅ `path` alias `@/*` 정상. `from '@/lib/api'` 같은 깔끔한 import.
- ✅ MainMap이 가중치 상태 단일 소유 → Sidebar는 controlled component.
- ✅ ReactQueryDevtools 추가 (DEBUG 시).

## Cross-Reference Notes (Mode E)

- ✅ Score 컴포넌트는 5단계 동네 패널 종합 점수에 그대로 재사용 가능.
- ✅ 색 매핑(`scoreToHeatmapColor`)은 다른 시각화에서도 일관 사용 가능.
- ✅ 가중치 표현(`Weights = {rent, amenity, transit}` 0~100 정수)은 백엔드 인터페이스와 정확 일치 → 5단계 selection learning에서도 같은 타입 재사용.

## 5단계 메모

backend-engineer 위임 시 다음을 명시:
1. **신규**: `GET /api/dongs/:slug/summary` (SPEC 6.2)
   - 응답 (TS):
     ```ts
     interface DongSummary {
       slug: string;
       name: string;
       gu: string;
       score: number; // weighted, default 33/33/34
       summary: string; // 한 줄 요약 (룰베이스, SPEC 11.3)
       rent_avg: number; // 만원
       nearest_station: { name: string; line: string; walking_min: number };
       amenity_level: 'sufficient' | 'normal' | 'lacking';
       single_household_pct: number; // 0~100
       safety_level: 'high' | 'mid' | 'low';
     }
     ```
2. **확장 (선택)**: `/api/dongs/scores` 응답에 `score_rent/score_amenity/score_transit` 3개 필드 추가 → SPEC 14.3 클라 재계산.
3. 한 줄 요약 generator (SPEC 11.3 룰베이스 10~15 템플릿).

frontend-engineer 5단계:
- DongPanel 컴포넌트 (우측 슬라이드 인 380~420px).
- HeatMap의 `onDongClick`을 패널 오픈으로 교체 (현재는 콘솔 로그).

## 최종 결정

**PASS** → git commit 진행. N1 자체 수정. N2~N5는 5단계 이후 트래킹.
