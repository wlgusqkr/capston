# QA Review: 5단계-B 프론트 동네 패널

리뷰 모드: **Mode B (Frontend Screen Review) + Mode E (Cross-Screen Consistency)**
리뷰어: 메인 코디네이터
일자: 2026-05-02

## Verdict

**PASS WITH NOTES** — 1건 자체 수정 후 통과.

## Scope

### 검토 파일
- 신규: `frontend/src/components/Map/DongPanel.{tsx,css}`
- 수정: `frontend/src/types/api.ts`, `lib/api.ts`, `hooks/useDongs.ts`, `routes/MainMap.tsx`

### Ground truth
- SPEC 6.2 (동네 패널 명세)
- 5단계-A 핸드오프 (API 응답 형식)

## 정적 + 런타임 검증

| 검사 | 결과 |
|---|---|
| `tsc --noEmit` | ✅ |
| `npm run build` | ✅ 222 modules, 132KB gz JS / 12KB gz CSS |
| Hex 하드코딩 in feature code | ✅ 0건 |
| `font-size: ...px` 하드코딩 | ⚠ 1건 → **자체 수정 완료** |
| `localStorage` / `sessionStorage` | ✅ 0건 |
| TanStack Query (`useDongSummary` enabled by `!!slug`) | ✅ |
| 백엔드 API 호출 동작 | ✅ /pildong/summary, /scores 둘 다 200 |
| Vite dev 서버 + Django 동시 가동 | ✅ |
| MainMap 통합 (`onDongClick` → setSelectedSlug) | ✅ `MainMap.tsx:111` DongPanel 마운트 |

## Findings

### Blockers
- 없음.

### Notes (자체 수정 또는 트래킹)

#### N1. ~~`DongPanel.css:86` 닫기 글리프 `font-size: 22px` 하드코딩~~ — 수정 완료
- **위치**: `frontend/src/components/Map/DongPanel.css:86`
- **문제**: 닫기 (×) 버튼 글리프 22px 직접 지정. 토큰 `--font-h2-size: 22px` 동일 값 존재.
- **조치**: `var(--font-h2-size)`로 교체. typecheck/build 재통과.

### Good

- ✅ 점수 구성 raw 점수를 `MainMap`의 `useDongScores` 결과에서 `useMemo` lookup → DongPanel에 prop 전달. 중복 query 방지 (효율적 + 가중치 변경 시 자동 갱신).
- ✅ `useDongSummary({ enabled: !!slug })` — slug null이면 query 비활성. 패널 닫혔을 때 불필요한 호출 없음.
- ✅ DongPanel은 닫힘 상태에서도 DOM 유지 + transform translateX로 슬라이드 → 애니메이션 부드러움.
- ✅ ESC 키 닫기 + 닫기 버튼 동작.
- ✅ 한국어 라벨 일관: 편의시설 "충분/보통/부족", 안전 "높음/보통/낮음".
- ✅ 토큰 `--transition-base/slow` 사용 (SPEC 6.1의 "300ms" 트랜지션과 일관).
- ✅ Card variant="inset" + Score size="lg" + Badge 활용 (디자인 시스템 재사용).

## Cross-Reference Notes (Mode E)

- ✅ Score 컴포넌트의 자동 색상 매핑 (점수 구간) 활용 — 메인 지도와 시각 언어 일관.
- ✅ Badge variant 매핑(success/warning/danger)이 amenity_level/safety_level과 정합.
- ✅ 가중치는 MainMap이 단일 소유 → DongPanel은 controlled. 슬라이더 변경 시 패널의 score/summary도 자동 갱신 (queryKey 포함).

## 6단계 메모

backend-engineer 위임 시:
- `GET /api/dongs/:slug/detail` 신규 (SPEC 6.3)
- 6개 섹션 데이터 한 번에 (Hero, RealEstate, Amenity, Transit, Review, SimilarDongs)

frontend-engineer 5단계 후속:
- `useNavigate('/dong/:slug')` 라우트는 6단계에서 구현됨. 현재는 NotFound로 가도 OK.
- 비교/찜하기는 alert. 8/9단계에서 활성화.

## 최종 결정

**PASS** → git commit 진행.
