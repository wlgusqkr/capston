# 변경 이력

## 2026-05-18
- OpenTelemetry 분산 추적 추가 — Tempo로 trace export. 백엔드 응답이 느릴 때 Grafana에서 해당 요청의 SQL/Redis/HTTP 호출까지 한 화면에서 추적 가능

## 2026-05-17
- Prometheus 메트릭 노출 추가 — `django-prometheus` 통합, `/metrics` 엔드포인트 신설. 홈서버 모니터링 스택(Prometheus + Grafana)이 백엔드 응답 시간 p50/p95/p99과 DB·캐시 메트릭을 수집하도록 함

## 2026-05-15
- TopNav 개편 — 서비스명 "자취맵"으로 변경, 맵/대시보드 메뉴를 왼쪽 정렬 개별 버튼으로, AI 버튼 rounded-full 테두리 스타일 적용
- AI 사이드 패널 닫기 버튼을 패널 왼쪽 면 슬라이딩 핸들로 변경 — 열기/닫기 토글 지원, 부드러운 cubic-bezier 애니메이션
- TransitSection 미사용 변수(busStopCount) 제거로 CI 타입 에러 해결
- 해석 텍스트를 초록색 chip 스타일로 변경 — 연한 초록 배경 + 초록 글자 둥근 pill 형태 (10곳, 5개 파일)
- 면적×환산월세 산점도 툴팁 "만원만원" 중복 표기 수정
- 대시보드 해석 텍스트(insight) 스타일 업그레이드 — 13px→14px + font-medium으로 가독성 강화, 부동산 섹션에 월세 수준 해석 텍스트 신규 추가
- 안전 등급 레이더 차트 높이 2배 확대(140px->280px) + 교통사고 바 차트 Y축 라벨 잘림 수정(width 88->100)
- 교통 섹션에서 '1인당 차량 등록' 카드 삭제, 안전·경제 섹션 같은 행 위젯 높이 통일(items-start 제거), 교통·안전 섹션 위젯 타이틀 폰트 16px 상향
- 인기 차트 섹션 4개 위젯을 한 행(grid-cols-4)에 배치하고, 인기·인구·리뷰 섹션 위젯 타이틀 폰트를 16px로 통일
- 부동산 시세 주택 유형 도넛 차트 1.5배 확대, 편의시설 '자취생 필수시설' 칩 그리드 삭제, 부동산·편의시설 위젯 타이틀 폰트 16px로 상향
- 교통 섹션 상단 그리드 빈 공간 최적화 — 지하철역 TOP 3(col-span-2)과 버스 세로 2칸을 3열 균등 배치로 변경
- 부동산 시세 섹션 빈 공간 제거 — KPI행 4열→2열(빈 div 삭제), 차트 그리드 4열→2열(2x2 균등 배치), 보증금 대역 col-span 제거
- 인기 차트·안전 섹션 빈 공간 최적화 — placeholder/empty state의 고정 높이를 패딩으로 교체하고, 안전 레이더·GRDP 행에 items-start 적용하여 카드 간 높이 불균형으로 생기던 빈 공간 제거
- 편의시설 도서관 placeholder 고정 높이(80px) 제거 — 콘텐츠에 맞는 py-6 패딩으로 교체

## 2026-05-14
- 대시보드 QA 라운드 2 — 폰트·차트·그리드 크기 대폭 축소, 모든 위젯에 한국어 해석 텍스트 추가, 비교군(percentile/rank) 뱃지 확대
- KPI 카드에 서울 동 대비 percentile 뱃지 + 한줄 해석(월세 수준, 보증금 부담, 거래 활발도, 안전 등급) 추가
- 자취촌 지수 breakdown 텍스트 잘림(truncate) 수정 — 자연 줄바꿈 허용
- 부동산 시세 그리드를 2열→4열로 변경, 월세추이 col-span-2 + 유형분포·산점도·보증금 각 1칸
- 인구·사회 3행→2행 압축 (도넛+바 4열, 청년+연령+1인가구 3열), 인사이트 3종 추가
- 안전·환경·경제 교통문화+GRDP 3열 분리, 녹지+화재 4열, 인사이트 4종 추가
- 교통 섹션 차트 높이 축소 + 대중교통 접근성 인사이트 추가
- 편의시설 충분/부족 요약 인사이트 + 테이블·칩 폰트 축소
- 인기 차트 2×2 그리드 개편 — 종합 점수 TOP 10 + 자취촌 지수 TOP 10(placeholder) + 학교별 empty state + 비슷한 동
- 리뷰 섹션 empty state 개선 ("아직 리뷰가 없어요" + 작성 유도 문구)
- 전체 위젯 출처 라벨에서 DB 컬럼명(rent_deal, gu_metric, ACC_TOTAL_COUNT 등) 제거
- 안전·환경·경제 섹션 컴팩트화 — 차트 높이·폰트 축소, GRDP 카드 2장 분리, 출처 라벨 간결화, 안전/사고율/녹지/경제 인사이트 텍스트 4종 추가
- 인구·사회 섹션 레이아웃 개선 — 3행을 2행으로 압축, 폰트·차트 높이 축소, 인구 추세·청년 비율·1인 가구 인사이트 텍스트 3종 추가
- 대시보드 QA: 전체 위젯 컴팩트화 (패딩·간격·차트 높이 일괄 축소, 10개 파일)
- 지하철역 TOP 3 중복 해소 — 동일 역명은 하나로 합치고 호선을 전부 표시
- 인구 추이 차트를 전년 대비 증감률 막대그래프로 교체 (평평한 라인 → 변화량 강조)
- 교통사고 위젯에 인구 10만명당 사고율 추가, 25구 평균 비교 가능하도록 개선
- 고령인구비율 도넛과 1인당 녹지 카드 삭제
- 위젯별 데이터 출처를 기관명·DB 테이블·metric 코드 수준으로 상세화
- 데이터 소스 문서(.claude/DATA_SOURCES.md) 신규 작성 — 섹션별 위젯 출처·계산 방법·알려진 문제 정리

## 2026-05-13
- 대시보드 KPI 행에 '자취촌 지수' 게이지와 '계약 활발도' 카드를 추가 — 동별 비아파트·소형·월세 비중 가중평균과 인구 대비 12개월 거래량을 한눈에, 백분위 뱃지로 서울 426동 중 상위 위치 표시
- 대시보드 §4.5 '자취촌 지수' + '계약 활발도' 위젯용 API (`GET /api/dongs/<slug>/derived-indices`) 추가 — 최근 12개월 rent_deal을 426개 동에 한 번에 집계해 0.5×비아파트+0.3×≤25㎡+0.2×월세건수 정규화 공식으로 자취촌 지수(0~100, 백분위·순위 포함) 산출, 인구 대비 거래량으로 계약 활발도 계산, 5시간 dict 캐시(warm <100ms)
- 대시보드 교통 섹션에 지하철·버스 시간대 혼잡도 라인 차트 2종과 동 성격 추정 카드(주거 중심 / 상업·업무 중심 / 유동인구 많음, 출근·낮·퇴근·주말 4개 패턴 막대 포함) 추가 — 신규 transit-congestion API 연동
- 대시보드 §4.4 섹션 C '시간대 혼잡도' + §4.5 '동 성격 추정' 위젯용 API (`GET /api/dongs/<slug>/transit-congestion`) 추가 — 가까운 지하철역 TOP 3 평일/토/일 24시 평균 + 동 매핑 버스 정류장 평일/주말 24시 평균(최근 60일) + 출퇴근·낮·주말 비율 기반 personality(주거 중심 / 상업·업무 중심 / 유동인구 많음 / null), 5분 캐시
- 대시보드 편의시설 섹션에 '대형 공원' 카드 리스트 추가 — 면적 내림차순 TOP 6, ha/㎡ 자동 환산 + 도보 분 표기, 중복 공원 행 자동 정리, 도서관은 "데이터 준비 중" placeholder 유지
- 대시보드 §4.4 섹션 B '대형 공원' 위젯용 API (`GET /api/dongs/<slug>/parks`) 추가 — 행정동에 매핑된 공원 전체를 면적 내림차순으로 반환, 거리(m)는 행정동 중심점↔공원 위치 ST_DistanceSphere
- 동·구 비교를 SeoulMetric raw → 25구 평균/순위로 정정. 추이 차트 스케일 정상화 + 카드에 자치구 순위 노출
- 구별 지표 API(`/gu-metrics`, `/gu-metrics/series`) 응답에 25개 자치구 산술 평균 + 본 구의 25구 중 순위를 추가 (`rank_in_seoul`/`gu_count`/`gu_avg`, `gu_avg_series`, `current_rank`) — 기존 `seoul_avg`는 서울시 전체 합/대표값이라 "다른 구 대비" 비교가 불명확하던 문제 해결, 응답 캐시 키 v2로 갱신
- 대시보드 안전·환경·경제 섹션에 교통사고 추이·화재 발생 추이 라인 차트 2종 추가 (구 + 서울 평균 비교, 10년치 연간 데이터, 좌→우 그려짐 애니메이션)
- 구별 지표 시계열 API (`/api/dongs/<slug>/gu-metrics/series`) 추가 — 교통사고/화재/지가 변동률 등 추이 차트용 다중 코드 + 최근 N년 응답
- 별점 리뷰 컬러를 노란색으로 통일, Badge 작은 사이즈와 KPI 카드 패딩 일관화, 음주·뺑소니 비율 막대 색을 차트 전용 warning 톤으로 정리 (디자인 일관성)
- 대시보드 인구·사회·안전·환경·경제·교통·부동산 섹션을 35종 gu-metric 실데이터에 전면 연결하고, 평균 연령·고령 비율·교통문화지수 레이더·1인당 차량·지가 변동률·주택 수 위젯 6종을 새로 추가
- 구별 지표 API(`/api/dongs/<slug>/gu-metrics`) 응답을 metric_code별 최신 행으로 확장 — 청년 비율 / 안전 등급 / 교통사고 / 녹지 / GRDP / 화재 등 35종이 한 번에 채워져 인구·사회 + 안전·환경·경제 섹션 데이터 누락 해결
- **Phase 3**: 인기 차트 섹션 (서울 TOP10 / 학교별 TOP5 / 인근 비슷한 동, 동 전환 + 비교하기 링크)
- **Phase 3**: 자취생 리뷰 섹션 (평균 별점 카운트업 + 별 5개 / 대표 리뷰 가로 스크롤 / 리뷰 작성 CTA)
- 대시보드 전체 폰트 Pretendard 통일 (mono-label → text-caption, --font-mono 오버라이드)
- 대시보드 배경색 grey → green-wash 전환 (bg-surface-alt → bg-primary-soft)
- 보증금 대역 막대그래프 색상 primary-green으로 변경
- 편의시설 테이블 헤더 글꼴·배경 수정 (font-mono 제거, 그린워시 적용)
- **Phase 2**: 인구·사회 섹션 (남녀 비율 도넛/인구 추이 라인/청년 비율 카드/1인 가구 추정)
- **Phase 2**: 안전·환경·경제 섹션 (6분야 안전등급 레이더/교통사고 통계/녹지·GRDP·화재 지표, 구 단위 뱃지)
- 대시보드용 인구 시계열 API (`/api/dongs/<slug>/population`)와 구별 지표 API (`/api/dongs/<slug>/gu-metrics`) 추가
- 카테고리 컬러 토큰 6종, 히트맵 레이어 팔레트 5종 (각 5단계), shimmer 키프레임 추가
- Gauge 원형 게이지 프리미티브 추가 (차오르는 애니메이션)
- 대시보드 라우트(`/dashboard`) 추가, 동 셀렉터 + 섹션 placeholder 셸 구현
- TopNav에 맵/대시보드 네비탭 + AI 검색 버튼 추가
- AI 사이드 패널 shell 구현 (mock 응답, 레이아웃 시프트 방식)
- **Phase 1**: 대시보드 KPI 행 (환산월세/보증금/거래건수/안전게이지, 카운트업 애니메이션)
- **Phase 1**: 미니맵 (히트맵 6종 레이어 토글 + 컬러칩 범례 + 확장 버튼 + 동 클릭)
- **Phase 1**: 부동산 시세 섹션 (월별 추이 라인/유형 도넛/산점도/보증금 바, Recharts 애니메이션)
- **Phase 1**: 편의시설 섹션 (카테고리별 테이블 + 충분도 뱃지 + 자취생 필수시설 칩 그리드)
- **Phase 1**: 교통 섹션 (지하철역 TOP3 + 호선 색상 + 버스 정류장/노선 수)

## 2026-05-12 — 사이드바 CSS 깨짐 수정 + QA 이슈 해결
- Chip UI 프리미티브 추가, 사이드바 필터 칩/라디오/레인지 슬라이더 스타일 복원
- Slider 프리미티브 thumb 정렬 버그 수정 (원이 바 아래로 치우치던 문제)
- Slider track gradient를 Slider 전용으로 스코프 (필터 슬라이더에 50% 채우기 노출 방지)
- Button disabled 색상, Legend 히트맵 토큰, 임의 px값 토큰 전환 등 QA 지적사항 일괄 수정
- apt 차트 색상을 CHART_COLORS로 통합

## 2026-05-12 — 디자인 시스템 v2 + Tailwind CSS 전환
- 컬러 토큰 시맨틱 리네이밍: 61개 → 25개 + 14개 불변 = 39개
  - 구체적 색상명(near-black, ink, hairline 등) → 역할명(secondary, text, divider 등)
- MetricBar 컬러: score (#FB6666/#FFD82A/#059669/#C1CCFF/#5570F1), weight → primary
- Status/MetricBar 색상 통합 (danger/warning/success/info)
- Tailwind CSS v4.3.0 설치, @theme 토큰 매핑
- 컴포넌트 CSS 파일 40+ → 0개 (globals.css 단일)
  - 모든 스타일 JSX className으로 인라인 전환
  - @keyframes, Leaflet 오버라이드, DivIcon 스타일은 globals.css 유지
- tokens.css 삭제 → globals.css @theme 통합
- 폰트: Pretendard only (Space Grotesk, Inter 제거)
- colors.ts hex 동기화
- CSS 크기: 128KB → 69KB (46% 감소)

## 2026-05-09
- 실데이터 ETL 완료: 426개 행정동, 740만 거래, 53만 상점, 800만 버스혼잡
- STUDIO MATCH 필터 구현
- MAP MODE 2옵션 정리 (매칭/종합점수)

## 2026-05-08
- RDS 연동 Django 모델 확장: 11개 신규 모델
- 디자인 시스템 쇼케이스 페이지 추가

## 2026-05-04
- 디자인 폴리시 v2: TopNav, 동네 패널 슬라이드, 비교 페이지 하이라이트

## 2026-05-03
- Cohere-inspired 디자인 피벗
- 거래 핀/클러스터, 커널 점수 API, 동네 탐색 대시보드

## 2026-05-02
- 프로젝트 초기 구축 완료 (9단계)
